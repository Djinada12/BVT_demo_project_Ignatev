spring:
  application:
    name: BVT_demo1
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    password: Djinada2160
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  port: 8080